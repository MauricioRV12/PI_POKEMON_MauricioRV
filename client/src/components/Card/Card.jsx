import './Card.css';
import { React } from 'react';
import { Link } from 'react-router-dom';

const Card = (pokemon, id) => {
    return (
        <div>
            
        </div>
    )
};

export default Card;